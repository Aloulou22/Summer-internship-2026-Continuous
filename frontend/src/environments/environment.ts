export const environment = {
  production: true,
  // Single entry point for every backend call — the API Gateway.
  // No service is ever addressed directly from the frontend.
  apiUrl: 'https://api.taskflow.example.com',
};
